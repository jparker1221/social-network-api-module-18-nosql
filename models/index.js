// Import User and Thought models
const User = require('./User');
const Thought = require('./Thought');

// Export User and Thought models for use elsewhere in the application
module.exports = { User, Thought };